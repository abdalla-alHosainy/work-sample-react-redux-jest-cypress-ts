import * as React from "react"
import styled from "@emotion/styled"
function Hero() {
  return <Section id="hero-container">Hero</Section>
}
const Section = styled.section`
  position: absolute;
  width: 93.8vw;
  height: 95vh;
  overflow: hidden;
  background-color: #fff;
`

export default Hero
