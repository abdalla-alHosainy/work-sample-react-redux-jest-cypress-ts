import * as React from "react"
import styled from "@emotion/styled"
function Charts() {
  return <Section id="charts-container">Charts</Section>
}
const Section = styled.section`
  position: absolute;
  width: 93.8vw;
  height: 95vh;
  overflow: hidden;
  background-color: #fff;
`

export default Charts
